// Placeholder Google Maps link used for every plot page's "Directions"
// button until real per-layout/per-city coordinates are provided. Once real
// location data comes in, set a `directionsUrl` on the specific layout
// entry (src/data/layouts.js) or city entry (CITY_META in src/data/plotMap.js)
// to override this default — no component changes needed.
export const DEFAULT_DIRECTIONS_URL = 'https://maps.app.goo.gl/SGSwyfNjHAiEqJBHA'
